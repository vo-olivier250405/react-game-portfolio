class Ressources {
  constructor() {
    // All assets to download
    this.toLoad = {
      sky: "/sprites/sky.png",
      ground: "/sprites/ground.png",
      hero: "/sprites/hero-sheet"
    };
    // Place to stack all images
    this.images = {};

    Object.keys(this.toLoad).forEach(key => {

    })
  }
}
